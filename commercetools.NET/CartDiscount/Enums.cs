namespace commercetools.CartDiscount
{
    public enum CartDiscountType
    {
        Relative,
        Absolute,
        GiftLineItem
    }

    public enum CartDiscountTargetType
    {
        LineItems,
        CustomLineItems,
        Shipping
    }
}
